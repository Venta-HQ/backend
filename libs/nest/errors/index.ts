// Export the error handling system
export { AppError, ErrorType } from './error';
export { ErrorCodes, interpolateMessage, type ErrorCode } from './error-codes';

// Export the exception filter
export { AppExceptionFilter } from '../filters/exception.filter';

// Export the error handling module
export { ErrorHandlingModule } from './error-module';
